---
// THE BLACK BOX. Added 2026-09-17, rewritten 2026-09-18.
//
// WHAT CHANGED AND WHY. The first version was written as a demo page: here is a
// replay, here are the controls. That undersold it. Four sector pages already
// promise this artefact, the mechanism behind it is the company's entire claim,
// and a page that opens with a play button asks the reader to work out its
// significance for themselves.
//
// THE FRAMING, AND THE TRAP IN IT. The obvious headline — "the black box is
// open, see inside the neural network" — is the one sentence that would lose a
// technical reader on the first screen. The full technical account draws the
// line itself: structural telemetry, which component failed and when and under
// what bound, is NOT semantic interpretability, what a given unit encodes. This
// page never claims the second. Claiming it would be the same overreach every
// other page here exists to avoid, and it is unnecessary, because there is a
// larger true claim available.
//
// THE CLAIM THIS PAGE MAKES. The black box was never one problem; it is two
// questions that were given one name. What does this model KNOW is semantic,
// genuinely hard, and open. What did this model DO to itself, and which part did
// what, is an accounting question — and it is the one that gates audits,
// validation, deletion and change control. For a decade both were attacked the
// same way, by reverse-engineering a finished network. For the second question
// that was the wrong instrument, because an account that was never written
// cannot be recovered. Change how the network trains and it writes one.
//
// So the page argues: the opacity is not a law of deep learning, it is a
// consequence of one design decision. Split the problem in two. One half is
// still open and this page says so plainly. The other half is closed, and the
// replay is that half running.
//
// The objections are not a FAQ. They are what a technical evaluator raises in
// the first five minutes, answered before they are asked, with the two limits
// first.
import Page from '../layouts/Page.astro';
import Eyebrow from '../components/Eyebrow.astro';
import Accordion from '../components/Accordion.astro';
import ReplayDemo from '../components/ReplayDemo.astro';
import TourGate from '../components/TourGate.astro';
import DemoTour from '../components/DemoTour.astro';

// The social card is generated from the bundle by scripts/build-replay-cards.py
// and rasterised by scripts/rasterise-cards.sh, which needs librsvg. Point
// og:image at it only once the PNG actually exists: a card URL that 404s is
// worse than the site default, because the link then previews as nothing at all.
// Resolved at build time by Vite, so no filesystem types are needed.
const cards = import.meta.glob('/public/black-box/cards/*.png');
const cardPath = Object.keys(cards).some((k) => k.endsWith('dead-layer-lesion.png'))
	? '/black-box/cards/dead-layer-lesion.png'
	: undefined;

const objections = [
	{
		id: 'obj-adverse',
		q: 'Where does it lose?',
		a: [
			'Under adversarial weight injection the architecture measures 1.0 percentage point worse than a parameter-matched baseline, and there is no current mitigation. Adversarially crafted perturbations hold nominal activation statistics while moving decision boundaries, which evades precisely what this monitors.',
			'That run is in the scenario selector. It plays, and it loses.',
		],
	},
	{
		id: 'obj-deepsup',
		q: 'Is this deep supervision with extra steps?',
		a: [
			'Auxiliary classification heads on intermediate layers are long established, and the local loss shares that lineage. Two things differ. The readout is a single shared low-rank bottleneck across every node rather than independent per-layer classifiers, which couples readout capacity across the network and makes the signal a continuous health baseline rather than a training objective. And no prior architecture couples that isolation with a health-gated closed-loop repair mechanism.',
			'The coupling is the contribution, not the auxiliary loss.',
		],
	},
	{
		id: 'obj-cifar',
		q: 'This is CIFAR-10.',
		a: [
			'Every result is CIFAR-10 or CIFAR-100. No clinical, biological, financial or defence data has ever touched this system. That is the binding constraint on the whole programme rather than a detail, and it is stated on every page where a result appears.',
		],
	},
	{
		id: 'obj-overhead',
		q: 'What does the instrumentation cost in compute?',
		a: [
			'Two numbers, and conflating them would flatter the answer. In parameters the cost is a bounded constant that does not grow with the model. In wall-clock throughput it is not a constant: the full system has been measured at 1.35× on a graph backbone and 2.16× on a CNN, and the gap between those two is unfused Python loops rather than algorithmic complexity.',
			'Most of the 1.35× is gradient surgery, which monitoring does not need. The read-only path runs none of it, and its wall-clock cost has been characterised as negligible but not separately benchmarked, so it is a measurement we will run against the customer configuration rather than quote. Anyone sizing a frontier run should assume the full figure until that measurement exists.',
		],
	},
	{
		id: 'obj-simulation',
		q: 'Is this a simulation?',
		a: [
			'No. It is a replay of one archived training run. The run identifier and the seed are on screen at every frame, and the figures are the values recorded during that run.',
			'Playback speed varies across the run so that the failure is legible rather than a blink. The data is untouched; only the camera speed changes.',
		],
	},
	{
		id: 'obj-transformer',
		q: 'Has this been run on a transformer?',
		a: [
			'No. The architecture is demonstrated on fully-connected, convolutional and residual families. Transformers are a stated generalisation target, not a demonstrated result.',
		],
	},
	{
		id: 'obj-scale',
		q: 'Does the replay prove it works at scale?',
		a: [
			'No, and that is not what it is for. It shows one 200-epoch run on a network of 636,677 parameters, and what it establishes is the mechanism rather than the scale. Frontier scale, regulated data and production over time are outside the evidence: no deployment exists, and nothing here is asserted about behaviour under any of the three.',
		],
	},
	{
		id: 'obj-rollback',
		q: 'Why not checkpoint and roll back instead?',
		a: [
			'Rollback recovers the accuracy and discards every step of legitimate learning since the last checkpoint, and it arrives with no account of what failed. The cost of the remedy is set by the checkpoint interval rather than by the size of the fault, and the same failure remains available to happen again unexplained.',
			'The replay is not an argument that repair beats rollback on accuracy. It is an argument that a named component, a step and a magnitude is a different class of object from a collapsed curve.',
		],
	},
	{
		id: 'obj-code',
		q: 'Where is the code?',
		a: [
			'The mechanism is released in full on publication, and that commitment is written into the licence rather than promised. Free for research, teaching and evaluation from that day, permanently.',
			'Until then, evaluation access is available under a short agreement, every archived run regenerates from its seed, and the falsification specification is supplied on request.',
		],
	},
	{
		id: 'obj-commercial',
		q: 'What is the commercial model?',
		a: [
			'One product, one licence. The mechanism publishes free for research, teaching and evaluation, permanently. Production deployment is licensed: annual, per institution, running on customer infrastructure, with the bound calibrated for the domain, reproducible builds, support and indemnity.',
			'Nothing is priced per feature and there is no price list. Terms are agreed per institution and written into the contract.',
		],
	},
];

// Two questions that were given one name. Splitting them is the page's
// argument, and the left column has to be written with real respect: it is a
// serious research programme that is honest about its own difficulty, and
// sneering at it would be both wrong and transparently self-serving.
const halves = [
	{
		id: 'semantic',
		state: 'OPEN',
		question: 'What does this model know?',
		gloss: 'What a given component encodes. Which concepts live where. Why this input produced that output.',
		body: [
			'This is the question mechanistic interpretability works on, and it is a serious programme — named a breakthrough technology of 2026 and producing real results.',
			'It is also honest about its own difficulty. Published surveys record that current work remains largely confined to toy models or to small fragments of larger networks, and that there is no agreed way to tell a true explanation from a merely plausible one. The substrate resists the question: a single unit routinely encodes several unrelated features at once, so there is no clean correspondence between the parts of a network and the concepts anyone wants to ask about.',
			'Nothing on this page closes this question, and nothing on this site claims to.',
		],
	},
	{
		id: 'structural',
		state: 'CLOSED',
		question: 'What did this model do to itself?',
		gloss: 'Which component failed. At which step. By how much. Inside what declared limit. Traceable to the data that caused it.',
		body: [
			'This is not a question about meaning. It is a question about accounting, and it is the one that decides whether a model can be validated, audited, updated under a change-control plan, or have a data source removed from it on request.',
			'It was attacked with the same instrument as the question above — examine the finished network, estimate what must have happened — and that instrument cannot answer it. An account that was never written during training cannot be recovered from the weights afterwards. The information was not hidden. It was never separable.',
			'Bound each component\u2019s learning path and it becomes separable, so the network writes the account as it goes. The replay below is that account being written.',
		],
	},
];

const obligations = [
	{ date: '17 April 2026', what: 'SR 26-2 supersedes SR 11-7. In force.', past: true },
	{ date: '2 December 2027', what: 'EU AI Act, Annex III obligations apply.', past: false },
	{ date: '2 August 2028', what: 'EU AI Act, Annex I transition.', past: false },
	{ date: '25 March 2029', what: 'European Health Data Space, secondary use.', past: false },
];

// ONE TRAIL, TWO CONSUMERS: the banner a reader sees and the BreadcrumbList a crawler
// reads. They used to be declared separately, in two shapes, and could not be kept in step —
// see the `trail` prop in layouts/Page.astro for what that cost.
const trail = [{ label: 'Home', href: '/' }, { label: 'The black box' }];

---

<Page
	title="The black box is not a law of nature"
	description="Opacity is not inherent to deep learning. It is a consequence of one design decision, made early and never revisited. Change the decision and half the problem stops existing — here is that half, running."
	ogImagePath={cardPath}
	trail={trail}
>
	{/* NO BANNER ON THIS PAGE. Every other page opens with one; this one opens with the
	    product. A 490px hero in front of an application is the "boxed into a website" look —
	    a visitor arriving here should already be inside the instrument, and the writing is
	    underneath it for whoever scrolls. The breadcrumb moved into the app bar so the page
	    keeps its BreadcrumbList. */}
	{/* FULL BLEED, NO CONTAINER, NO SECTION PADDING. In app mode the replay is not a figure
	    inside a column — it is the screen. Wrapping it in .container would box an application
	    into a 1280px measure with cream either side, which is the "boxed into a place on a
	    website" look this replaces. */}
	{/* Decides the landing — a tour link, the offer to watch the demo, or the plain page —
	    before the console can start. Must stay above it. There is no title sequence any more
	    (removed 2026-09-24): the offer is the first thing a visitor sees, and the demo's own
	    opening does the work the title sequence did. */}
	<TourGate />

	

	<div class="demo-app">
		<ReplayDemo scenario="dead-layer-lesion" guide="auto" app trail={trail} />
	</div>

	{/* "Play the demo": the self-driving walkthrough of the console above. /black-box only,
	    desktop only; it drives the real console rather than a recording of it. Placed after
	    the console so its parse-time gate runs before the console's module script. */}
	<DemoTour />

	{/* THIS SECTION NO LONGER INTRODUCES THE SCREEN — the stripe at the top of the console
	    does that, and it says: two networks, the same data, the same damage; one names what
	    broke and repairs itself, the other never can; every figure comes from one archived
	    run. Four of those clauses were repeated here almost word for word, which is what a
	    reader meets as padding. What is left is what the stripe does NOT carry: the specific
	    conditions of this run, and how to check that the screen is reading an archive rather
	    than performing one. */}
	<section class="section container">
		<Eyebrow>THE RUN, REPLAYED</Eyebrow>
		<h2>What was done to the network, and how to check it.</h2>
		<p class="demo-intro">
			Both arms have identical shape and were trained on the same data toward the same
			objective. At epoch 101 two of their convolutional stages are destroyed outright. The
			sealed arm does not degrade gracefully: it falls to chance and stays there for the rest
			of the run.
		</p>
		<p class="demo-intro">
			The controls drive an archive, not a simulation. Play and the epoch bar move both arms
			through the same recorded run, and the table underneath stops answering the same way
			for the two of them. The run's identifier and seed sit in the bar at the top of the console, and each of
			the four scenarios regenerates from its own seed. The fourth goes against us, and it
			sits in the same menu as the other three.
		</p>
		{/* THIS BELONGS TO THE PARAGRAPHS ABOVE IT. It had a <section> to itself, which on this
		    page means 80px of padding above and 80px below — 160px of empty cream between a
		    note and the copy it annotates, while the page's other three notes sit 24px under
		    theirs. The orphan section is gone; the spacing now matches every other note. */}
		<p class="demo-note demo-note--tight">
			Nothing in the sealed pane was withheld. A standard network does not compute a
			per-component health signal at any point in training, so there is no quantity to display
			and no setting that would reveal one. That absence is the problem, drawn.
		</p>
	</section>

	<section class="section container">
		<Eyebrow>THE SPLIT</Eyebrow>
		<h2>The black box was never one problem. It is two questions that were given one name.</h2>
		<p class="demo-intro">
			What the open pane just did is not a better reading of a finished model. It is a
			different question, answered by different means — and the distinction is not academic.
			One of these is a question about meaning, and it is genuinely hard. The other is a
			question about accounting, and it is the one that decides whether a regulated
			institution can use a model at all. For a decade both were attacked with the same
			instrument.
		</p>
		<div class="demo-halves">
			{halves.map((h) => (
				<article class={`demo-half demo-half--${h.id}`}>
					<p class="demo-half-state">{h.state}</p>
					<h3>{h.question}</h3>
					<p class="demo-half-gloss">{h.gloss}</p>
					{h.body.map((para) => <p>{para}</p>)}
				</article>
			))}
		</div>
		<p class="demo-note">
			Only the right-hand question is closed here, and only within the limits set out below.
			A page that claimed both would be worth less, not more, to the people who have to file
			against it.
		</p>
	</section>

	<section class="section container">
		<Eyebrow>WHY THE OLD INSTRUMENT COULD NOT DO IT</Eyebrow>
		<h2>An account that was never written cannot be recovered from the weights.</h2>
		<p class="demo-intro">
			Every error signal in a standard neural network reaches every parameter on every step.
			That property is the source of a network's capability, and it is also why nothing inside
			one can be named afterwards. The trail does not go cold. It was never laid.
		</p>
		<p class="demo-intro">
			Ask a finished model what it took from a particular source and every available tool works
			the same way: examine the trained system, produce a best estimate of what must have
			occurred. The estimate is frequently a good one. It is also unverifiable, two tools
			disagree on the same input, and an estimate is not something a data owner accepts or a
			supervisor files.
		</p>
		<p class="demo-intro">
			This was never a tooling failure. The quantity was not being withheld. In a network where
			every gradient touches every parameter, there is no component whose behaviour can be
			isolated from the aggregate, so there is nothing for a question to be put to. Reverse
			engineering is the right instrument for the left-hand question and the wrong one for the
			right-hand question, and the field spent a decade using it for both.
		</p>
		<p class="demo-intro">
			That decision was reasonable when it was made and has not been revisited since. Change
			it — bound each component's learning path so the component stays individually
			addressable — and one half of the black box stops existing. Not because a better tool
			was pointed at a finished model, but because the model keeps the account as it goes,
			which is what the run above is doing.
		</p>
	</section>

	<section class="section container">
		<Eyebrow>WHAT THAT RECORD IS FOR</Eyebrow>
		<h2>The same record answers four different people.</h2>
		<p class="demo-intro">
			It is one artefact, generated once, during training. What changes between these readers is
			the obligation that makes them ask.
		</p>
		<ul class="demo-readers">
			<li>
				<strong>The engineer whose run died.</strong> A component, a step and a magnitude, while
				the run is still recoverable — instead of a loss curve that moved once the damage was
				thousands of steps old.
			</li>
			<li>
				<strong>The validator who cannot verify a memo.</strong> A signed, per-modification
				record with a declared bound, diffable against the version last approved. Evidence of
				what training did rather than a written account of what the team intended.
			</li>
			<li>
				<strong>The reviewer assessing a model allowed to update.</strong> A declared set of
				modification types with individual ceilings, the record of each one as it happened, and
				the diff. Those are the three things a change-control plan asks for.
			</li>
			<li>
				<strong>The counsel holding a deletion clause.</strong> What the model took from a named
				source, held as an addressable part rather than spread across every parameter — the
				precondition for removing it and certifying what was removed.
			</li>
		</ul>
		<p class="demo-note demo-note--tight">
			Transparency is the mechanism. The product is that data a custodian will not release today
			becomes data they can release, because the account they have been waiting for now exists.
		</p>
	</section>

	<section class="section container">
		<Eyebrow>BEFORE THE QUESTIONS</Eyebrow>
		<h2>Ten objections, answered before they are raised.</h2>
		<p class="demo-intro">
			These are the questions a technical evaluation asks in its first five minutes, in the words
			a reader actually uses. The limits come first, because a page that lists only what worked
			is a marketing page wearing a lab coat.
		</p>
		<div class="demo-objections">
			{objections.map((o) => (
				<Accordion id={o.id} question={o.q}>
					{o.a.map((p) => <p>{p}</p>)}
				</Accordion>
			))}
		</div>
	</section>

	<section class="section container">
		<Eyebrow>THE BOUNDARY OF THE CLAIM</Eyebrow>
		<h2>Stated here, in the same place as the claim.</h2>
		<p class="demo-intro">
			A page arguing that half of a decade-old problem is closed has to say exactly which half
			and exactly how far, in the same breath. These are the boundaries of the evidence.
		</p>
		<ul class="demo-limits">
			<li>
				<strong>This is structural transparency, not semantic interpretability.</strong> The
				record names which component failed, when, by how much and inside what limit. It does
				not say what that component encodes, and no claim here should be read as saying it does.
			</li>
			<li>
				<strong>Every result is CIFAR-10 or CIFAR-100.</strong> No clinical, biological,
				financial or defence data has ever touched this system. That is the binding constraint
				on the whole programme rather than a detail.
			</li>
			<li>
				<strong>Transformers are a stated target, not a demonstrated result.</strong>
				Demonstrated on fully-connected, convolutional and residual families.
			</li>
			<li>
				<strong>Where it loses.</strong> Under adversarial weight injection the architecture
				finishes 1.0 percentage point behind a parameter-matched baseline, with no current
				mitigation; that run is in the scenario selector above, and it plays, and it loses. It also
				finishes 0.9 pp behind under a 100× weight explosion, and on ResNet-18 an uninstrumented
				network recovers slightly better (92.6% vs 91.7%).
			</li>
			<li>
				<strong>The claim stops where the evidence stops.</strong> Frontier scale, regulated data
				and production over time are outside it — no deployment exists, and nothing here is
				asserted about behaviour under any of the three.
			</li>
		</ul>
		<p class="demo-note demo-note--tight">
			The claim is not that a network can now be read. It is that a single-network architecture
			can produce mechanistically logged, per-component causal attribution that no prior
			single-network method offers, and that this is what makes recovery from failures possible
			that standard architectures cannot even detect.
		</p>
	</section>

	<section class="section container">
		<Eyebrow>WHY THE TIMING MATTERS</Eyebrow>
		<h2>The obligations arrive on legislated dates, not forecast ones.</h2>
		<ul class="demo-dates">
			{obligations.map((o) => (
				<li class={`demo-date${o.past ? ' demo-date--past' : ''}`}>
					<span class="demo-date-when">{o.date}</span>
					<span class="demo-date-what">{o.what}</span>
				</li>
			))}
		</ul>
		<p class="demo-note demo-note--tight">
			An architecture cannot be retrofitted into a model that has already been trained. An
			institution that begins the architecture question after the date is not late by a quarter;
			it is late by a training cycle.
		</p>
	</section>

	<section class="section container">
		<Eyebrow>THREE WAYS ON</Eyebrow>
		<h2>Start with the problem already on the table.</h2>
		<div class="demo-ctas">
			<article class="demo-cta">
				<h3>Run the falsification specification</h3>
				<p>
					The remaining research programme comes down to four measurements, each with a
					written prediction, against a specification we supply. It runs on customer hardware, by the
					institution's own technical partner, without us in the room.
				</p>
				<a class="demo-cta-link" href="/contact?intent=falsify#falsify">Request the specification</a>
			</article>
			<article class="demo-cta">
				<h3>Evaluate it directly</h3>
				<p>
					Evaluation access is available now under a short agreement, and free and unrestricted
					once the research release is out. Either way it runs entirely in the customer
					environment, with nothing reaching us.
				</p>
				<a class="demo-cta-link" href="/contact?intent=other#other">Ask for access</a>
			</article>
			<article class="demo-cta">
				<h3>Pre-book a deployment</h3>
				<p>
					Terms are agreed per institution and written into the contract, and pre-booking fixes
					them ahead of general availability. Early partners shape the baseline architecture
					while the requirements are still open.
				</p>
				<a class="demo-cta-link" href="/contact?intent=pre-book#pre-book">Pre-book a deployment</a>
			</article>
		</div>
	</section>

	{/* THE READING LIST IS GONE, 2026-09-18. It cited no figure on this page. It was three
	    outbound links supporting a rhetorical claim about the state of the field, rendered as
	    a bulleted list of blue underlines — the visual language of a Wikipedia references
	    section, at the foot of the page that has to carry the most conviction.

	    It also worked against the page's own argument. The page is careful to say it does not
	    attempt the semantic question that mechanistic interpretability attempts; a reading
	    list of mechanistic interpretability papers invites exactly the comparison the section
	    above it spends four paragraphs declining. And the last thing this page should do is
	    send a reader somewhere else.

	    The sourcing that remains is the sourcing that carries figures: the sector pages
	    footnote external numbers they quote as fact, and that stays. */}

	<section class="section container">
		<p class="demo-foot">
			Every figure on this page is measured on CIFAR-10 across the controlled experiment
			programme built by founder <a href="/about#founder">Rokib Al Dhin Raadh</a>, and is labelled
			as such wherever it appears.
			<a href="/technology">The technology page</a> carries the conditions for each, the error
			bars, and every run where it loses.
		</p>
	</section>
</Page>

<style>
	.demo-intro {
		max-width: 62ch;
		font-size: var(--text-body);
		line-height: var(--lh-body);
		color: var(--text-secondary);
	}
	.demo-intro--muted { font-size: var(--text-small); color: var(--text-muted); }

	/* VERTICAL RHYTHM. Measured down the rendered page rather than guessed: every block pair
	   on this page sits 24px apart — intro to grid, grid to note, list to note — with exactly
	   two exceptions, and both were 0px. A heading landed flat on its first paragraph in all
	   seven sections, and consecutive paragraphs had no gap at all, so the four under "An
	   account that was never written" read as one undivided block. The rule below puts those
	   two pairs on the same 24px the rest of the page already uses. */
	.section h2 + .demo-intro,
	.demo-intro + .demo-intro { margin-top: var(--space-lg); }

	.demo-lede {
		max-width: 64ch;
		font-size: var(--text-body);
		line-height: var(--lh-body);
		color: var(--text-primary);
		margin-bottom: var(--space-md);
	}

	/* THE SPLIT. The two halves are given identical weight on purpose: the
	   left-hand question is a serious programme, and a layout that made it look
	   like the loser of a comparison would be both wrong and transparently
	   self-serving. Only the state chip differs. */
	.demo-halves {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
		gap: var(--space-lg);
		/* --space-xl (40px) made this the only grid on the page that did not open 24px under
		   its paragraph; .demo-readers, .demo-limits and .demo-objections all use --space-lg. */
		margin: var(--space-lg) 0;
	}
	.demo-half {
		border: 1px solid var(--border-hairline);
		border-radius: 4px;
		padding: var(--space-lg);
		background: var(--bg-surface);
	}
	.demo-half h3 { margin: var(--space-sm) 0; font-size: var(--text-h3); }
	.demo-half p {
		font-size: var(--text-small);
		line-height: var(--lh-body);
		color: var(--text-secondary);
		margin-bottom: var(--space-sm);
	}
	.demo-half-state {
		font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
		font-size: var(--text-eyebrow);
		letter-spacing: 0.12em;
		color: var(--text-muted);
	}
	.demo-half-gloss {
		font-weight: 600;
		color: var(--text-primary) !important;
	}
	.demo-half--structural { border-color: color-mix(in srgb, var(--chart-ormas) 40%, transparent); }
	.demo-half--structural .demo-half-state { color: var(--chart-ormas); font-weight: 700; }

	.demo-readers, .demo-limits {
		list-style: none;
		padding: 0;
		margin: var(--space-lg) 0 0;
		max-width: 76ch;
	}
	.demo-readers li, .demo-limits li {
		padding: var(--space-md) 0;
		border-bottom: 1px solid var(--border-hairline);
		font-size: var(--text-small);
		line-height: var(--lh-body);
		color: var(--text-secondary);
	}
	.demo-readers li strong, .demo-limits li strong { color: var(--text-primary); }

	/* The open half carries no accent: identical weight to its neighbour is the
	   point, and only the state chip distinguishes them. */
	.demo-half--semantic { }

	/* FLUSH TO THE NAV. A 24px top margin here put page background between the header and
	   the console, so the cream band read as 112px on this page against 89px everywhere
	   else — the nav looked taller when the nav had not changed at all. An application
	   screen starts where the chrome above it ends. */
	.demo-app { margin-block: 0; }

	.demo-note {
		max-width: 68ch;
		font-size: var(--text-body);
		line-height: var(--lh-body);
		color: var(--text-secondary);
		border-left: 2px solid var(--border-strong);
		padding-left: var(--space-md);
		margin: 0;
	}
	.demo-note--tight { margin-top: var(--space-lg); }

	.demo-objections { margin-top: var(--space-lg); }

	.demo-dates { list-style: none; padding: 0; margin: var(--space-lg) 0 0; }
	.demo-date {
		display: grid;
		grid-template-columns: 11rem 1fr;
		gap: var(--space-md);
		padding: var(--space-sm) 0;
		border-bottom: 1px solid var(--border-hairline);
		font-size: var(--text-small);
	}
	.demo-date-when {
		font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
		color: var(--text-primary);
		font-weight: 600;
	}
	.demo-date-what { color: var(--text-secondary); }
	.demo-date--past .demo-date-when { color: var(--text-muted); }

	.demo-ctas {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
		gap: var(--space-lg);
		margin-top: var(--space-lg);
	}
	.demo-cta {
		border: 1px solid var(--border-hairline);
		border-radius: 4px;
		padding: var(--space-lg);
		background: var(--bg-surface);
	}
	.demo-cta h3 { margin: 0 0 var(--space-sm); font-size: var(--text-h3); }
	.demo-cta p {
		margin: 0 0 var(--space-md);
		font-size: var(--text-small);
		line-height: var(--lh-body);
		color: var(--text-secondary);
	}
	.demo-cta-link { font-size: var(--text-small); font-weight: 600; }

	.demo-foot {
		max-width: 72ch;
		font-size: var(--text-small);
		color: var(--text-muted);
		line-height: var(--lh-body);
	}

	@media (max-width: 600px) {
		.demo-date { grid-template-columns: 1fr; gap: 2px; }
	}
</style>
